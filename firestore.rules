rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Development-only permissive rules for emulator.
    // WARNING: Do NOT use these in production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    function signedIn() {
      return request.auth != null;
    }

    // App-development rules:
    // Any signed-in Firebase Auth user can use the app collections.
    // This avoids profile-discovery deadlocks when a Firestore user profile
    // exists by email/student id instead of by Firebase Auth UID.
    match /users/{userId} {
      allow read, write: if signedIn();
    }

    match /tenants/{tenantId} {
      allow read, write: if signedIn();

      match /products/{productId} {
        allow read, write: if signedIn();
      }
    }

    match /inventoryBatches/{batchId} {
      allow read, write: if signedIn();
    }

    match /inventoryTransactions/{transactionId} {
      allow read, write: if signedIn();
    }

    match /businessContacts/{contactId} {
      allow read, write: if signedIn();
    }

    match /purchaseOrders/{purchaseOrderId} {
      allow read, write: if signedIn();
    }

    match /sales/{saleId} {
      allow read, write: if signedIn();
    }

    match /{document=**} {
      allow read, write: if false;
    }
  }
}
